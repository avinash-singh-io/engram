# OS
.DS_Store

# Node / TypeScript (package lands in Phase 0)
node_modules/
dist/
build/
*.log
.env
.env.*

# momentum runtime sentinels (transient — not tracked)
.momentum/brainstorm-active
.momentum/merge-approved
.momentum/runs/
.momentum/inbox/
.momentum/.state/

# gate1 extracted corpus — contains raw prompt text, never committed (ADR-0037)
.gate1/

# gate2 corpus and judgements — contains vault content, never committed
.gate2/
