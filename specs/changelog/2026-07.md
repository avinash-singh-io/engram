# Changelog — 2026-07

> One line per change, append-only, newest at the bottom.

- 2026-07-03 — Scaffolded Engram from the PRD via `/start-project`: vision (charter, principles, success criteria), roadmap (Phase 0 + PRD Phases 1–5).
- 2026-07-03 — Recorded 7 ADRs (0001–0007): separate product/shared engine, OKF v0.1, standard links, git source of truth, navigate-first retrieval, auto-indexes, TypeScript single-package.
- 2026-07-03 — Added light architecture overview (`specs/architecture/overview.md`).
- 2026-07-03 — Planned Phase 0 — Foundation (package + toolchain + OKF format core); seeded backlog (FEAT-001/002, TD-001, ENH-001).
- 2026-07-03 — Started Phase 0 — Foundation on branch `phase-0-foundation`.
- 2026-07-03 — Phase 0 Group 0: bootstrapped TypeScript ESM package + toolchain + CI (tsup/vitest/eslint/prettier); fixed ESM-vs-CJS git-hook conflict (TD-002).
- 2026-07-03 — Phase 0 Group 1: defined OKF v0.1 conformance spec (`docs/okf-conformance.md`).
- 2026-07-03 — Phase 0 Group 2: format core — frontmatter validator, concept-ID resolver, locked v1 fixtures corpus (27 tests).
- 2026-07-03 — Phase 0 Group 3: commander CLI skeleton + 7 subcommand stubs (30 tests).
- 2026-07-03 — Phase 0 Group 4: full verification green (`npm run check` exit 0, 30/30 tests, CLI smoke) — phase verified, awaiting `/complete-phase`.
- 2026-07-03 — **Released v0.1.0** (Phase 0 — Foundation): TS/Node package + toolchain + CI, OKF v0.1 conformance spec, frontmatter validator + concept-ID resolver, CLI skeleton. npm publish deferred.
- 2026-07-03 — **Released v0.2.0** (Phase 1 — MVP Vault + Claude Code): landed to main.
- 2026-07-03 — Wave 2 built in **3 parallel lanes** (git worktrees) via background agents: Phase 2 (Retrieval, 122 tests), Phase 3 (Sync, 65), Phase 4 (Ecosystem, 94).
- 2026-07-03 — Phase 2 — Progressive-Disclosure Retrieval: `engram recall` navigator (bounded reads ~16% files, 0 body reads on eval), locked `recall-v1` benchmark (Rule 11), `RecallResult` export, AGENTS.md contract, FEAT-002 resolved (ADR-0010). **Landing as v0.3.0.**
- 2026-07-03 — **Released v0.4.0** (Phase 4 — Ecosystem): Codex + Antigravity adapters (shared `COMMAND_DEFINITIONS`; a new agent is a descriptor, ADR-0011), `/promote` momentum→OKF bridge (validate-gated, provenance, ADR-0012). Rebased onto main after Phase 2; ADRs renumbered 0011/0012. 164 tests.
- 2026-07-03 — Parallel-brainstormed the whole roadmap (6-agent workflow); wrote `specs/planning/parallel-execution-plan.md` (3 waves, file-conflict matrix, shared foundations, lane plan). Running full-auto.
- 2026-07-03 — Started Phase 1 — MVP Vault + Claude Code on branch `phase-1-mvp-vault` (Wave 1).
- 2026-07-03 — Phase 1 built (G0–G8): vault fs layer, deterministic indexer, write-hook (ADR-0008), `.engram/` sidecar (ADR-0009), register-hook CLI dispatch + adapter seam, commands init/capture/refine/link/reindex, bundled assets, Obsidian doc. 52 tests, `npm run check` exit 0.
- 2026-07-03 — Phase 1 verified; **v0.2.0 merge pending** user approval (full-auto Wave 1 gate).
- 2026-07-03 — Started Phase 3 — Sync + Multi-Device on branch `phase-3-sync` (Wave 2 lane, parallel with Phases 2/4).
- 2026-07-03 — Phase 3 built (G1–G6): `docs/sync/` recipes (git-spine, Obsidian Git [canonical free path], Remotely Save→S3, verified round-trip) + vault.gitignore/s3-iam-policy assets; new read-only `engram doctor` command (reuses validateConcept + reindex --check; conflict/CRLF/BOM/case-fold/git checks); automated round-trip fidelity test (git-clone + S3-copy transports, adversarial CRLF/BOM/NFD); ADR-0013 (canonical free path), ADR-0014 (M5 instrument). 65 tests, `npm run check` exit 0. Real-device Android screenshots pending.
- 2026-07-03 — **Released v0.5.0** (Phase 3 — Sync + Multi-Device): rebased onto main (ADRs renumbered 0013/0014); 177 tests. **Wave 2 complete** — 3 parallel lanes landed 2 → 4 → 3. M5 real-device evidence pending (automated round-trip proof shipped).
- 2026-07-03 — **Roadmap wrapped at v0.5.0** (Phases 0–4 complete). Phase 5 (Semantic Layer) deferred as an optional future wave. Core product shipped: `init`/`capture`/`refine`/`link`/`reindex`/`recall`/`promote`/`doctor` across Claude Code/Codex/Antigravity.
- 2026-07-03 — Published to npm as `@avinash-singh-io/engram` (v0.5.0 → v0.5.1 README fix); added tag→npm auto-publish CI workflow.
- 2026-07-03 — **Released v0.6.0** (Phase 6 — Onboarding & OKF Migration): editor-adapter layer (Obsidian; ADR-0015) + `init` auto-setup (editor config + `git init`); each agent adapter emits a native pointer (Claude → `CLAUDE.md`, fixing a real gap); `engram migrate` adopts existing notes as OKF concepts (frontmatter + wikilink conversion, ADR-0016); `CLAUDE.md` reserved. 191 tests, `npm run check` exit 0.
- 2026-07-03 — v0.6.1 `migrate` title heuristic (leading heading, else filename); v0.6.2 agent contract files carry the FULL contract, not a pointer (ADR-0017, amends 0011); v0.6.3 `engram init --refresh` re-renders tool-owned templates after an upgrade without touching user content.
- 2026-07-03 — v0.6.4: scaffold `.engram/okf-format.md` (authoritative concept-format reference the agent reads before writing; AGENTS.md/CLAUDE.md instruct it to); `engram migrate` now logs each adopted concept to `log.md`.
- 2026-07-04 — BUG-001 (ad-hoc quick-task, branch `fix/BUG-001-link-encoding`): link targets with spaces broke CommonMark parsing — Obsidian/GitHub rendered them broken and engram's own `MD_LINK_RE`/`BULLET_RE` dropped them entirely (spaced concepts vanished from `recall`/navigate). Fix: shared `encodeLinkTarget`/`decodeLinkTarget` in `src/format/links.ts` (per-segment percent-encoding, `#`-fragment-safe, idempotent, round-trips); encode at all 10 emit sites (indexer, log, link, recall, promote ×4, migrate); decode at the two parse boundaries (`extractMarkdownLinks`, `parseIndex`) → invariant "in-memory targets are decoded, encode only on write". 212 tests (19 new), CLI smoke + `doctor` clean. Scope: core round-trip only; legacy in-body links + doctor link-resolution check deferred.
