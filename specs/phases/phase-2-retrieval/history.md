# Phase 2 — History (append-only)

### [DECISION] 2026-07-03 — Bounded-read metric = body-tier reads; recall returns references
Topics: retrieval, bounded-read, recall-contract, okf-format
Affects-phases: phase-2-retrieval
Affects-specs: specs/decisions/0010-bounded-read-metric-and-recall-contract.md
Detail: Authored ADR-0010. M3/M6 bound only full-content (body-tier) reads; index/frontmatter/grep scans are bounded auxiliary I/O returning one-line-per-concept metadata. `recall` returns ranked references (path+title+description+why), not bodies — resolving FEAT-002. Extends ADR-0005; any index-tag inlining is an ADR-0006 amendment (deferred).

---

### [EVALUATOR] 2026-07-03 — Locked recall-v1 evaluator committed before tuning
Topics: retrieval, evaluator, rule-11
Affects-phases: phase-2-retrieval
Affects-specs: tests/benchmarks/recall-v1/README.md
Detail: Froze a 126-concept nested OKF fixture vault (20 directory indexes, generated by the real `reindex`) + a 10-query eval set with expectedConceptIds and per-query maxBodyReads/maxFilesFraction. Scalar = recall-correctness + body-reads. Committed before scoring was tuned (Rule 11); mutations go to recall-v2.

---

### [ARCH_CHANGE] 2026-07-03 — Structural descent reads O(directories) index maps
Topics: retrieval, navigation, adr-0006
Affects-phases: phase-2-retrieval
Affects-specs: specs/decisions/0005-navigate-first-retrieval.md, specs/decisions/0006-auto-generated-indexes.md
Detail: Because ADR-0006 section bullets are non-descriptive (`N concepts`), breadth-pruned descent can't reliably localize; the navigator instead BFS-reads all reachable index maps (the map) and opens zero/few bodies. This is sublinear in concepts (~16% of files touched on v1, 0 bodies). Logged as an observation on the ADR-0006 index format; no spec edit mid-phase (Rule 10).

---

### [ARCH_CHANGE] 2026-07-03 — AGENTS.md enriched additively, one writer util
Topics: agents-contract, vault-scaffold
Affects-phases: phase-2-retrieval, phase-1-mvp-vault
Affects-specs: assets/vault/AGENTS.md
Detail: Enriched the Phase-1 `assets/vault/AGENTS.md` baseline additively (read-budget + `engram recall` sections) rather than adding a separate `templates/AGENTS.md`. `renderAgentsContract()` reads the single-source asset; init and `recall --emit-contract` write byte-identical output. Shared-file edit the conductor reconciles at landing (parallel-plan §4 predicted this).

---

### [DISCOVERY] 2026-07-03 — TD-003: two vault-root discovery entry points
Topics: vault-root, tech-debt
Affects-phases: phase-2-retrieval
Affects-specs: specs/backlog/backlog.md
Detail: `resolveVaultRoot` (retrieval, `--vault` + exit-code semantics) wraps `findVaultRoot` (vault/paths). Two entry points with slightly different contracts; filed TD-003 (P3) to dedupe once Phase 1/2/3 vault access stabilizes.

---

### [NOTE] 2026-07-03 — cli-program dispatch loop untouched
Topics: cli-wiring, registry
Affects-phases: phase-2-retrieval
Affects-specs: none
Detail: Phase 1 already shipped the `register?()` hook + single dispatch loop, so wiring `recall` was a one-entry registry change; `src/cli-program.ts` was NOT edited (design.json G6 anticipated an edit that Phase 1 pre-empted). Only shared edits: `registry.ts` (recall entry), `tests/cli.test.ts` (stub guard), `assets/vault/AGENTS.md`, `src/index.ts` (barrel export).

---
